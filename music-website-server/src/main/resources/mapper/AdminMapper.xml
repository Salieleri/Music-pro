<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.salieri.dao.AdminMapper" >
  <resultMap id="BaseResultMap" type="com.example.salieri.entity.Admin" >
    <id column="admin_ID" property="adminId" jdbcType="VARCHAR" />
    <result column="admin_password" property="adminPassword" jdbcType="VARCHAR" />
  </resultMap>
  <sql id="Base_Column_List" >
    admin_ID, admin_password
  </sql>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.String" >
    select
    <include refid="Base_Column_List" />
    from admin_info
    where admin_ID = #{adminId,jdbcType=VARCHAR}
  </select>
  <select id="login" parameterType="String" resultType="int">
    select count(*)
    from admin_info
    where admin_ID = #{Admin} and admin_password = #{pswd}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.String" >
    delete from admin_info
    where admin_ID = #{adminId,jdbcType=VARCHAR}
  </delete>
  <insert id="insert" parameterType="com.example.salieri.entity.Admin" >
    insert into admin_info (admin_ID, admin_password)
    values (#{adminId,jdbcType=VARCHAR}, #{adminPassword,jdbcType=VARCHAR})
  </insert>
  <insert id="insertSelective" parameterType="com.example.salieri.entity.Admin" >
    insert into admin_info
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="adminId != null" >
        admin_ID,
      </if>
      <if test="adminPassword != null" >
        admin_password,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="adminId != null" >
        #{adminId,jdbcType=VARCHAR},
      </if>
      <if test="adminPassword != null" >
        #{adminPassword,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>
  <update id="updateByPrimaryKeySelective" parameterType="com.example.salieri.entity.Admin" >
    update admin_info
    <set >
      <if test="adminPassword != null" >
        admin_password = #{adminPassword,jdbcType=VARCHAR},
      </if>
    </set>
    where admin_ID = #{adminId,jdbcType=VARCHAR}
  </update>
  <update id="updateByPrimaryKey" parameterType="com.example.salieri.entity.Admin" >
    update admin_info
    set admin_password = #{adminPassword,jdbcType=VARCHAR}
    where admin_ID = #{adminId,jdbcType=VARCHAR}
  </update>
</mapper>
